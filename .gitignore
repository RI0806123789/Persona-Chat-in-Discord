# --- Secrets: NEVER commit these ---
config_private.py
*.env
.env

# --- Python ---
__pycache__/
*.pyc
*.pyo

# --- Temp files generated by the bot at runtime ---
temp_speech_*.mp3

# --- OS junk ---
.DS_Store
Thumbs.db

# --- Third-party binaries: too large for git (avcodec-62.dll alone is
#     ~100MB, over GitHub's single-file limit). Download FFmpeg separately
#     instead of committing it. ---
ffmpeg-master-latest-win64-gpl-shared/
ffmpeg.exe
ffplay.exe
ffprobe.exe
*.dll


# --- Runtime memory files ---
memory/*
!memory/.gitkeep
*temp

# --- NG ---
NG_WORD_private.txt
ng/blacklist.json
*temp

memo.txt